<h2>New Post</h2>

<%= for_for @post do |p| %>
	<fieldset>
		<div class="field">
			<%= p.label :body %>
			<%= p.text_field :body %>
		</div>
		<div class="actions">
			<%= p.submit %>
		</div>
	</fieldset>
<% end %>	